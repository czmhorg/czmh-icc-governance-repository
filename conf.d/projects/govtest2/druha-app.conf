# Nastavení repa czmh-icc-govtest2-druha-app (defs/defs.md, nastavení repa): přepisuje
# vybrané klíče projektu govtest2. Klíč zde neuvedený = platí hodnota projektu.
# Povolené klíče: rulesets, pr_reviewers_team, repository_teams_add.
# Rulesety tohoto repa (přepisuje rulesets projektu); none|jenkins = bez rulesetů
# mh-policy-*, Jenkins bot jako collaborator.
rulesets=none|jenkins
# Týmy navíc k repository_teams projektu (CSV <tým>|<právo>, nepovinné).
# repository_teams_add=
# Tým pro automatické žádosti o review (přepisuje pr_reviewers_team projektu, nepovinné).
# pr_reviewers_team=
