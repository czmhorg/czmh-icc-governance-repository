# Nastavení repa czmh-icc-govtest1-pata-app (defs/defs.md, nastavení repa): přepisuje
# vybrané klíče projektu govtest1. Klíč zde neuvedený = platí hodnota projektu.
# Povolené klíče: rulesets, pr_reviewers_team, repository_teams_add.
# Rulesety tohoto repa (přepisuje rulesets projektu), CSV položek <profil>[|jenkins].
rulesets=no-pr
# Tým pro automatické žádosti o review (přepisuje pr_reviewers_team projektu);
# none = žádný tým.
pr_reviewers_team=none
# Týmy navíc k repository_teams projektu (CSV <tým>|<právo>, nepovinné).
# repository_teams_add=
